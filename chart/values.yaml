replicaCount: 1

image:
  repository: docker.io/mrjoees/nethack
  tag: "1.0.4"
  pullPolicy: IfNotPresent

service:
  port: 7681

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

uds:
  host: nethack
  gateway: tenant

additionalNetworkAllow: []
